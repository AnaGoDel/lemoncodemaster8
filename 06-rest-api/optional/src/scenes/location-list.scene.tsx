import * as React from 'react';
import { AppLayout } from 'layouts';
import { LocationListContainer } from 'pods/location-list';

export const LocationListScene = () => (
  <AppLayout>
    <LocationListContainer />
  </AppLayout>
);
